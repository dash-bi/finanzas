# ---------------------------------------------------------------
# Construcción anterior, superada por la app actual. Se conserva
# en disco pero no se publica: un segundo index.html competiría
# con el de la raíz, que es el que despliega Vercel.
# ---------------------------------------------------------------
/_version-anterior/

# ---------------------------------------------------------------
# Build paralela ajena a este repositorio (creada el 09/09/2026).
# No se publica para no mezclar dos implementaciones distintas.
# Si debe formar parte del proyecto, quitar esta línea.
# ---------------------------------------------------------------
/assets/JS/modules/

# --- Secretos: nunca deben llegar al repositorio ---
.env
.env.*
*.pem
*.key
*.p12
*.pfx
secrets.json
credentials.json
config.local.*

# --- Respaldos exportados desde la aplicación (contienen datos reales) ---
respaldo-erp-*.json

# --- Ruido del sistema operativo y del editor ---
Thumbs.db
desktop.ini
.DS_Store
*~
*.tmp
*.bak
.vscode/
.idea/
